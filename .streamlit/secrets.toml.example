# Copy to .streamlit/secrets.toml for local runs, or paste the same contents
# into the Streamlit Community Cloud dashboard under App settings -> Secrets.
# Never commit the real secrets.toml.
GROQ_API_KEY = "your_groq_api_key_here"
