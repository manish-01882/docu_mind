[project]
name = "smart-doc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "manish-01882", email = "man01882@outlook.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
smart-doc = "smart_doc:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
